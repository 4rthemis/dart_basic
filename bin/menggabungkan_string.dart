/**
 * Menggabungkan String
 * 
 * kadang ada kebutuhan kita perlu menggabungkan ebberapa data string
 * untuk menggabungkan beberapa data String,
 * kita bisa menggunakan karakter + (tambah)
 * atau jika datanya tidak dalam bentul variable,
 * kita bisa langsung tambahkan hanya dengan karakter whitespace (spasi, enter, tab)
 */ ///

void main() {
  String fullName = 'abdul' ' gavin ' ' aja';

  print(fullName);
}
